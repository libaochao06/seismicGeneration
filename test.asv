clear all;
clc;
dt=0.01;
T=38.34;
deltaFreq=0.02442;
omegac=2*pi*deltaFreq;
freq=1.60221/2/pi;
omega=2*pi*freq;
t=[0:dt:20];
for i=1:length(t)
    a(i)=-1.2691*(sin(omegac*(t(i)-T))/omegac/(t(i)-T))^4*cos(omega*(t(i)-T));
end
plot(t,a);