clear all;
clc;
dt=0.01;
T=18.94;
omegac=0.0125664;
deltaS=0.0750719;
omega=0.628319;
t=[0:dt:40.95];
for i=1:length(t)
    a(i)=1*(sin(omegac*(t(i)-T))/omegac/(t(i)-T))^5*cos(omega*(t(i)-T));
end
plot(t,a);

damp=0.02;
deltaFreq=1/40.95;
omega0=omega/sqrt(1-damp^2);
for i=1:leng(t)
    ome=deltaFreq*2*pi;
    up=
